version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5001:5000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always