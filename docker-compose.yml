services:
  backend:
    build: ./backend
    ports:
      - "5001:5000"
    restart: always
    # ADD THESE TWO LINES:
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always